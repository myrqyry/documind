import type { Express } from 'express';
import type { DocuMind } from '../DocuMind';
import type { Multer } from 'multer';

export function setupRoutes(app: Express, documind: DocuMind, upload: Multer) {
  // TODO: Implement routes
}